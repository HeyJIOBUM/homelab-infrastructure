- name: Login to vault
  include_role:
    name: vault_login
  tags: always

- name: Bootstrap garage secrets
  include_tasks: 
    file: garage_secrets.yml
    apply:
      tags: bootstap-garage
  tags: bootstap-garage